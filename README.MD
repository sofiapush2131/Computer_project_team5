Поданий програмний модуль реалізує універсальний підхід до побудови та аналізу орієнтованих графів на основі алгоритму PageRank. Основною метою є створення гнучкого інструменту, який дає можливість працювати з будь-якою структурою даних, що може бути подана у вигляді графа: соціальними мережами, користувацькими взаємодіями, рекомендаційними системами чи мережевими структурами іншого типу. Код також включає механізми автоматичного тестування через doctest, що забезпечує коректність роботи всіх ключових функцій.

Першим логічним блоком є функція нормалізації матриці суміжності. Вона перетворює кожен стовпець матриці так, щоб його елементи сумарно дорівнювали одиниці. Така нормалізація необхідна для формування стохастичної матриці переходів, яка є базою для обчислення PageRank. Важливо, що функція належним чином обробляє стовпці з нульовою сумою, запобігаючи діленню на нуль.

Наступним елементом є реалізація алгоритму PageRank за допомогою степеневих ітерацій. Функція застосовує матрицю переходів, коефіцієнт демпінгу та механізм «телепортації» для знаходження стійкого стану розподілу значущості вузлів у мережі. Ітеративні обчислення тривають доти, доки різниця між двома послідовними характеристиками значущості не стане меншою за встановлену похибку. Отриманий результат є стаціонарним розподілом, що відображає відносний рівень важливості кожного елемента графа.

Окремий блок присвячено побудові графів. Функція створення орієнтованого графа формує матрицю суміжності на основі списків вузлів та ребер. Ребра задаються як пари «джерело–ціль», а їх відповідність відображається у вигляді значення 1 у відповідній позиції матриці. Паралельно формується словник індексів, що забезпечує швидкий доступ до позицій вузлів.

Функція формування рекомендацій інтегрує результати PageRank у прикладний контекст. Вона отримує на вході масив рангів, відповідність вузлів їхнім індексам та список кандидатів, серед яких необхідно здійснити вибір. Після сортування значень рангу функція повертає перелік елементів із найбільш високими показниками, що дозволяє застосовувати алгоритм для рекомендних систем.

Окремо реалізовано побудову двочасткового графа за взаємодіями категорій «користувач–об’єкт». Така структура широко використовується у системах рекомендацій, де дії користувача з певними елементами (наприклад, товарами, піснями чи статтями) утворюють двосторонні зв’язки. Функція формує симетричну матрицю суміжності між користувачами та об’єктами, що дозволяє моделі враховувати взаємний вплив під час обчислення PageRank.

На завершення модуль містить демонстраційний приклад використання. У ньому показано повний цикл: формування графа взаємодій, розрахунок вагових коефіцієнтів PageRank та отримання рекомендацій для конкретного користувача. Підключені doctests забезпечують автоматичну перевірку правильності роботи на стандартних тестових випадках, що підвищує надійність та відтворюваність результатів.


Тестування функцій
Цей модуль містить набір тестів, що перевіряють коректність роботи всіх ключових частин реалізації PageRank: нормалізації матриці, обчислення рангів, побудови графа та формування рекомендацій. Кожен тест охоплює окремий аспект, забезпечуючи стабільність та передбачуваність роботи алгоритму.
Функція normalize_matrix()
test_normalize_basic — перевіряє, що базова матриця нормалізується правильно по стовпцях.
test_normalize_column_sum — підтверджує, що кожен ненульовий стовпець після нормалізації має суму 1.
test_normalize_no_nan — гарантує відсутність значень NaN, навіть якщо в матриці присутні нульові стовпці.
Функція pagerank()
test_pagerank_symmetric — симетричні вузли отримують однакові значення PageRank.
test_pagerank_sum_one — перевіряє, що сумарний розподіл рангів є додатним і коректно сформованим.
test_pagerank_non_negative — усі PageRank-значення повинні бути невід’ємними.
test_dangling_node — вузли без вихідних ребер не повинні спричиняти помилки або створювати NaN.
test_all_zero_graph — у повністю порожньому графі всі вузли мають отримати однаковий ранг.
test_random_graph_stability — PageRank працює стабільно на випадкових матрицях і повертає правильний розподіл.
test_high_damping — алгоритм коректно функціонує при високому значенні коефіцієнта згладжування.
test_no_incoming_links_not_equal — вузли з різними структурами зв’язків повинні отримувати різні значення рангу.
Функція build_graph()
test_graph_structure — перевіряє правильність побудови двонаправлених зв’язків між user–item, коректність розміру матриці та відповідність зв’язків очікуваним даним.
Функція recommend()
test_recommend_not_user — користувач не повинен потрапляти у власний список рекомендацій.
test_recommend_sorted — результати повинні повертатися у порядку спадання PageRank.
test_popular_item — популярніші елементи мають отримувати вищий PageRank та відображатися вище у списку рекомендацій.

# PageRank Dockerized Project

# 🐳 Docker-інтеграція проєкту

Цей проєкт реалізує алгоритм PageRank на Python. Його контейнеризовано за допомогою Docker, що забезпечує стабільний запуск у будь-якому середовищі без потреби встановлювати Python або залежності вручну.
Замість довгих налаштувань — одна команда, і застосунок готовий до роботи 🚀

---

## Вимоги
- Встановлений Docker: [інструкції з установки](https://docs.docker.com/get-docker/)

---

## ⚙️ Вміст контейнера

- **Python 3.13-slim** як базовий образ
- Автоматичне встановлення залежностей із `requirements.txt`
- Копіювання всіх файлів у робочу директорію `/app`
- Автоматичний запуск `CLI.py` при старті контейнера

---

## 🧱 Dockerfile

```dockerfile
FROM python:3.13-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "CLI.py"]

```

## Як запустити проєкт через Docker

1. Клонуйте репозиторій та перейдіть у папку з проєктом:

git clone https://github.com/ViktoriiaTesliuk/Computer_project_team5?tab=readme-ov-file
cd repository/project

2. Збудуйте Docker-образ:

docker build -t pagerank-app .

3. Запустіть контейнер:

- Варіант 1 — передати команду вручну:

docker run pg python CLI.py --user=1 --top=3

- Варіант 2 — використовувати CMD із Dockerfile:

docker run pg -- --user=1 --top=3

(Тут `--` повідомляє Docker, що наступні аргументи потрібно передавати в Python-скрипт, а не інтерпретувати як параметри Docker.)

4. Після запуску ви побачите вивід роботи алгоритму у консолі.

---

## 🎯 Переваги Docker для цього проєкту

- Повна ізоляція середовища
- Передбачувана поведінка на будь-якій машині
- Швидкий деплой
- Відсутність конфліктів залежностей
- Зручність для командної розробки
